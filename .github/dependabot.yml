version: 2

# O Dependabot cobre o que o scan de dependências no CI fazia, e melhor:
# em vez de reprocessar a base de vulnerabilidades a cada pull request, ele
# monitora continuamente e abre um pull request com a correção quando algo
# aparece. Vulnerabilidade surge com o tempo, não com o commit.
updates:
  - package-ecosystem: maven
    directory: /auth-service
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    labels: [infra]
    commit-message:
      prefix: chore
      include: scope
    groups:
      # Atualizações de patch e minor viram um pull request só: revisar vinte
      # bumps triviais separados é o caminho mais curto para ninguém revisar.
      minor-and-patch:
        update-types: [minor, patch]

  - package-ecosystem: maven
    directory: /product-service
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    labels: [infra]
    commit-message:
      prefix: chore
      include: scope
    groups:
      minor-and-patch:
        update-types: [minor, patch]

  # As actions do pipeline também são dependências, e rodam com acesso ao
  # repositório. Uma action desatualizada é superfície de ataque de supply chain.
  # As actions são referenciadas por SHA de commit, e não por tag, porque tag é
  # ponteiro móvel: quem controla o repositório da action pode reapontá-la para
  # outro código e todo pipeline que a usa passa a executar isso sem ninguém
  # aprovar. O Dependabot entende o formato "sha # versão" e atualiza os dois,
  # que é o que torna a prática sustentável.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    labels: [infra]
    commit-message:
      prefix: chore
      include: scope

  - package-ecosystem: docker
    directories:
      - /auth-service
      - /product-service
    schedule:
      interval: weekly
      day: monday
    labels: [infra]
    commit-message:
      prefix: chore
      include: scope
